 <%= f.label :rating %>
 <% if rating %>
     <%= f.select :rating, options_for_select(Rating.severities.keys.to_a, rating.severity) %>
 <% else %>
     <%= f.select :rating, Rating.severities.keys.to_a %>
 <% end %>